what is pip?

exc_info=True
	Logs message and the full traceback info - helps debugging


# logging configuration
logging.basicConfig(
    filename='error_log.txt',       # Log file name
    level=logging.WARNING,            # Log only WARNING and above
    format='%(asctime)s - %(levelname)s - %(message)s'
)


if __name__ == "__main__": 
	only run the code under it if the file is called directly - not if the file is imported somewhere
	
	
	
	if create_new_user(row):
  print(f"User created: {row['email']}")
 else:
  logger.error(f"Error creating user: {row.get('email', 'unknown')}")
  print(f"Error creating user: {row.get('email', 'unknown')}")